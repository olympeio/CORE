import CreateColor from '../../../src/core/data/base/CreateColor.js';

describe('CreateColor brick', () => {
    it('should test the color, however, another behavioural from df, so no', () => {
        // Write your tests here
    });
});
