import GetUniqueRelatedObject from '../../../src/core/data/base/GetUniqueRelatedObject.js';

describe('GetUniqueRelatedObject brick', () => {
    it('should correctly retrieve the first object connected to the specified object by a relation', () => {
        // TODO
    });

    it('should return undefined if no object are connected to the specified object by a relation', () => {
        // TODO
    });
});
