import EndTransaction from '../../../src/core/data/transaction/EndTransaction.js';

describe('EndTransaction brick', () => {
    it('should do something', () => {
        // Write your tests here
    });
});
