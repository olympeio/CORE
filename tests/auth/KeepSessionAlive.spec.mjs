import KeepSessionAlive from '../../src/core/auth/KeepSessionAlive.js';

describe('KeepSessionAlive brick', () => {
    it('send a keep-alive notification to the DC', () => {
        // TODO
        // Run the brick and check that the DC got a notification
    });
});
